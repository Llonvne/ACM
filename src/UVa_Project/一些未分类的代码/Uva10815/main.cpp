#include "bits/stdc++.h"
using namespace std;
int main() {
    string lineBuffer;

    while(getline(cin, lineBuffer)){

        for (int i = 0; i < lineBuffer.length(); i++){

        }
    }
}
void deleteNotLetterAndTransformLowerCase(string )